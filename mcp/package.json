{
  "name": "@silver886/mcp-proxy-mcp",
  "private": true,
  "scripts": {
    "prebuild": "node -e \"const p=require('../package.json'); require('fs').writeFileSync('src/shared/generated.ts', 'export const PACKAGE_NAME = ' + JSON.stringify(p.name) + ';\\nexport const PACKAGE_VERSION = ' + JSON.stringify(p.version) + ';\\n')\"",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
